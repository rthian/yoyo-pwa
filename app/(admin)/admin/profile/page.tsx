/**
 * Admin account profile, inside the admin shell.
 * Linked from AdminHeader and AdminSidebar.
 */
import MemberProfilePage from '@/app/(member)/member/profile/page'

export default function AdminProfilePage() {
  return <MemberProfilePage embedded />
}
